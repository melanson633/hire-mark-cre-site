# Public Case Study Artifacts

This folder holds public-safe case-study deliverables copied from the `Job_Search` cousin workspace.

Each subfolder should contain only publishable artifacts, usually HTML and PDF exports. Minimal routing context belongs in `docs/source-context/ai-case-studies/`; structured site metadata belongs in `src/content/caseStudies/`.

Do not copy raw `00_user_context/` files here.
