# Source Context

This directory contains small copied context packets from the read-only cousin workspace:

`C:\Users\melan\OneDrive\Documents\Job_Search`

Use it to understand where imported public artifacts came from and what is safe to read next. It is not a runtime dependency and should not be imported by site code.

## Current Imports

- `ai-case-studies/` - routing index plus one context note per imported case-study artifact
- `ai-side-hustle/` - working strategy docs for productized AI service offers, including reusable AI automation consulting visual concept banks
- `linkedin-posts/` - production notes and approved caption context for imported LinkedIn research packages
